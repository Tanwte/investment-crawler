<!DOCTYPE html><html><head><meta charset="utf-8"><title>Edit URLs</title><link rel="stylesheet" href="/styles.css"></head>
<body><header><h1>Edit URLs</h1><nav><a href="/admin">Back</a></nav></header>
<main class="card" style="max-width:720px;margin:16px auto;">
<% if(error){%><div class="error"><%= error %></div><%} %><% if(success){%><div class="success"><%= success %></div><%} %>
<form method="POST" action="/admin/urls">
  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
  <p>One URL per line. Must be http(s). Required count: <strong><%= min %></strong> to <strong><%= max %></strong>.</p>
  <textarea name="urls" rows="16"><%= currentText %></textarea>
  <button type="submit">Save</button>
</form>
</main></body></html>