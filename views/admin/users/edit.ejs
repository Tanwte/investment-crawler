<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User - Investment Crawler Admin</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .form-container { max-width: 600px; margin: 20px auto; padding: 0 20px; }
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; margin-bottom: 5px; font-weight: 600; color: white; }
        .form-group input, .form-group select { width: 100%; padding: 10px; border: 1px solid #9ca3af; border-radius: 4px; font-size: 14px; background: #6b7280; color: white; }
        .form-group input:focus, .form-group select:focus { outline: none; border-color: #007bff; box-shadow: 0 0 0 2px rgba(0,123,255,0.25); }
        .btn-primary { background: #007bff; color: white; padding: 12px 24px; border: none; border-radius: 4px; cursor: pointer; font-size: 16px; }
        .btn-secondary { background: #6c757d; color: white; padding: 12px 24px; text-decoration: none; border-radius: 4px; display: inline-block; margin-right: 10px; }
        .btn-warning { background: #ffc107; color: #212529; padding: 12px 24px; text-decoration: none; border-radius: 4px; display: inline-block; margin-right: 10px; }
        .alert-danger { background: #f8d7da; color: #721c24; padding: 12px; margin-bottom: 20px; border-radius: 4px; border: 1px solid #f5c6cb; }
        .checkbox-group { display: flex; align-items: center; gap: 10px; }
        .checkbox-group input[type="checkbox"] { width: auto; }
        .user-info { background: #6b7280; color: white; padding: 15px; border-radius: 4px; margin-bottom: 20px; }
        .status-badge { padding: 4px 8px; border-radius: 12px; font-size: 11px; font-weight: bold; }
        .status-active { background: #d4edda; color: #155724; }
        .status-inactive { background: #f8d7da; color: #721c24; }
        .status-locked { background: #fff3cd; color: #856404; }
        .card { background: #6b7280; color: white; }
        h2, h3 { color: white; }
        small { color: #d1d5db !important; }
        input[disabled] { background: #4b5563 !important; color: #d1d5db !important; }
        .danger-zone { background: #6b7280; color: white; }
    </style>
</head>
<body>
    <header>
        <h1>Edit User: <%= editUser.username %></h1>
        <nav>
            <a href="/admin/users">← Back to User Management</a>
            <a href="/admin/users/<%= editUser.id %>/reset-password" class="btn-warning">Reset Password</a>
        </nav>
    </header>

    <div class="form-container">
        <% if (error) { %>
            <div class="alert-danger">
                <strong>Error:</strong> <%= error %>
            </div>
        <% } %>

        <div class="user-info">
            <h3>Current User Information</h3>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                <div>
                    <strong>User ID:</strong> <%= editUser.id %><br>
                    <strong>Username:</strong> <%= editUser.username %><br>
                    <strong>Current Role:</strong> 
                    <span class="status-badge <%= editUser.role === 'admin' ? 'status-locked' : 'status-active' %>">
                        <%= editUser.role.toUpperCase() %>
                    </span>
                </div>
                <div>
                    <strong>Status:</strong> 
                    <% if (editUser.account_locked) { %>
                        <span class="status-badge status-locked">LOCKED</span>
                    <% } else if (editUser.is_active) { %>
                        <span class="status-badge status-active">ACTIVE</span>
                    <% } else { %>
                        <span class="status-badge status-inactive">INACTIVE</span>
                    <% } %><br>
                    <strong>Failed Attempts:</strong> <%= editUser.failed_login_attempts || 0 %><br>
                    <strong>Last Login:</strong> 
                    <%= editUser.last_login ? new Date(editUser.last_login).toLocaleString() : 'Never' %>
                </div>
            </div>
        </div>

        <div class="card">
            <h2>Edit User Details</h2>
            <form method="POST" action="/admin/users/<%= editUser.id %>/edit">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                
                <div class="form-group">
                    <label for="username">Username (Read-only)</label>
                    <input type="text" id="username" value="<%= editUser.username %>" disabled 
                           style="background: #4b5563; color: #d1d5db;">
                    <small style="color: #d1d5db;">Username cannot be changed after creation.</small>
                </div>

                <div class="form-group">
                    <label for="full_name">Full Name</label>
                    <input type="text" id="full_name" name="full_name" 
                           placeholder="Enter full name" value="<%= editUser.full_name || '' %>">
                </div>

                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input type="email" id="email" name="email" 
                           placeholder="Enter email address" value="<%= editUser.email || '' %>">
                </div>

                <div class="form-group">
                    <label for="role">User Role</label>
                    <select id="role" name="role" required>
                        <option value="user" <%= editUser.role === 'user' ? 'selected' : '' %>>User - Regular access to crawler features</option>
                        <option value="admin" <%= editUser.role === 'admin' ? 'selected' : '' %>>Admin - Full system access and user management</option>
                    </select>
                    <% if (editUser.role === 'admin') { %>
                        <small style="color: #fbbf24;">⚠️ Be careful when changing admin roles.</small>
                    <% } %>
                </div>

                <div class="form-group">
                    <div class="checkbox-group">
                        <input type="checkbox" id="is_active" name="is_active" <%= editUser.is_active ? 'checked' : '' %>>
                        <label for="is_active">Account Active</label>
                    </div>
                    <small style="color: #d1d5db;">Inactive users cannot log in to the system.</small>
                </div>

                <div style="margin-top: 30px;">
                    <a href="/admin/users" class="btn-secondary">Cancel</a>
                    <button type="submit" class="btn-primary">Update User</button>
                </div>
            </form>
        </div>

        <% if (editUser.account_locked) { %>
            <div class="card" style="margin-top: 20px; border-left: 4px solid #ffc107;">
                <h3 style="color: #fbbf24;">🔒 Account Locked</h3>
                <p>This account is locked due to failed login attempts.</p>
                <form method="POST" action="/admin/users/<%= editUser.id %>/unlock" style="margin-top: 15px;">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit" class="btn-warning">Unlock Account</button>
                </form>
            </div>
        <% } %>

        <div style="margin-top: 30px; padding: 20px; background: #6b7280; color: white; border-radius: 4px; border: 1px solid #dc3545;">
            <h3>Account Management</h3>
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 15px;">
                <div>
                    <h4>🔑 Password Management</h4>
                    <p>Reset the user's password if they've forgotten it or need a new one.</p>
                    <a href="/admin/users/<%= editUser.id %>/reset-password" class="btn-warning">Reset Password</a>
                </div>
                <div>
                    <h4>🗑️ Account Deletion</h4>
                    <p>Permanently remove this user account from the system.</p>
                    <% if (editUser.id !== user.id) { %>
                        <form method="POST" action="/admin/users/<%= editUser.id %>/delete" style="display: inline;">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <button type="submit" class="btn-danger" 
                                    onclick="return confirm('Are you sure you want to delete user \'<%= editUser.username %>\'?\n\nThis action cannot be undone and will:\n- Remove the user account permanently\n- Invalidate all their sessions\n- Remove their audit log entries\n\nType \'DELETE\' to confirm.')">
                                Delete Account
                            </button>
                        </form>
                    <% } else { %>
                        <p style="color: #666; font-style: italic;">You cannot delete your own account.</p>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>