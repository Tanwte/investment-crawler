<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Users - Investment Crawler</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .create-section, .users-section {
            background: #0f1630;
            border: 1px solid #203065;
            border-radius: 8px;
            padding: 25px;
            margin-bottom: 30px;
        }
        
        .create-section h2, .users-section h2 {
            color: #f5b042;
            margin-top: 0;
            margin-bottom: 20px;
        }
        
        .form-row {
            display: flex;
            gap: 20px;
            margin-bottom: 15px;
            align-items: end;
        }
        
        .form-group {
            flex: 1;
        }
        
        .form-group label {
            display: block;
            color: #e8eefc;
            margin-bottom: 5px;
            font-weight: 500;
        }
        
        .form-group input, .form-group select {
            width: 100%;
            padding: 10px;
            background: #1a2a5a;
            color: #e8eefc;
            border: 1px solid #203065;
            border-radius: 4px;
            box-sizing: border-box;
        }
        
        .form-group input:focus, .form-group select:focus {
            outline: none;
            border-color: #f5b042;
            box-shadow: 0 0 0 2px rgba(245, 176, 66, 0.2);
        }
        
        .create-button {
            background: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.2s ease;
        }
        
        .create-button:hover {
            background: #45a049;
            transform: translateY(-1px);
        }
        
        .users-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        
        .users-table th {
            background: #1a2a5a;
            color: #f5b042;
            padding: 12px;
            text-align: left;
            border: 1px solid #203065;
            font-weight: bold;
        }
        
        .users-table td {
            padding: 12px;
            border: 1px solid #203065;
            color: #e8eefc;
            vertical-align: middle;
        }
        
        .users-table tr:nth-child(even) {
            background: rgba(26, 42, 90, 0.3);
        }
        
        .action-button {
            padding: 6px 12px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 12px;
            margin-right: 5px;
            transition: all 0.2s ease;
        }
        
        .reset-button {
            background: #ff9800;
            color: white;
        }
        
        .reset-button:hover {
            background: #f57c00;
        }
        
        .delete-button {
            background: #f44336;
            color: white;
        }
        
        .delete-button:hover {
            background: #d32f2f;
        }
        
        .role-badge {
            padding: 4px 8px;
            border-radius: 12px;
            font-size: 11px;
            font-weight: bold;
            text-transform: uppercase;
        }
        
        .role-admin {
            background: #f5b042;
            color: #050a12;
        }
        
        .role-user {
            background: #2196F3;
            color: white;
        }
    </style>
</head>
<body>
    <header>
        <h1>👥 Users Management</h1>
        <nav>
            <a href="/admin">Dashboard</a>
            <a href="/admin/keywords">Edit Keywords</a>
            <a href="/admin/urls">Edit URLs</a>
            <a href="/admin/users" class="active">Users</a>
            <a href="/admin/settings">Settings</a>
            <a href="/">← Back to Results</a>
            <form method="POST" action="/logout" style="display:inline;">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button>Logout</button>
            </form>
        </nav>
    </header>
    
    <div class="container">
        <div class="create-section">
            <h2>👤 Create User</h2>
            <form method="POST" action="/admin/users/create">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="form-row">
                    <div class="form-group">
                        <label>Username</label>
                        <input name="username" required>
                    </div>
                    <div class="form-group">
                        <label>Password</label>
                        <input name="password" type="password" required>
                    </div>
                    <div class="form-group">
                        <label>Role</label>
                        <select name="role">
                            <option value="user">User</option>
                            <option value="admin">Admin</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="create-button">✅ Create</button>
                    </div>
                </div>
            </form>
        </div>
        
        <div class="users-section">
            <h2>👥 Existing Users</h2>
            <table class="users-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Role</th>
                        <th>Created</th>
                        <th>Updated</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(u => { %>
                    <tr>
                        <td><%= u.id %></td>
                        <td><%= u.username %></td>
                        <td>
                            <span class="role-badge role-<%= u.role %>"><%= u.role %></span>
                        </td>
                        <td><%= new Date(u.created_at).toLocaleDateString() %></td>
                        <td><%= new Date(u.updated_at).toLocaleDateString() %></td>
                        <td>
                            <form method="POST" action="/admin/users/reset" style="display:inline;">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <input type="hidden" name="user_id" value="<%= u.id %>">
                                <button type="submit" class="action-button reset-button">🔄 Reset</button>
                            </form>
                            <form method="POST" action="/admin/users/delete" style="display:inline;">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <input type="hidden" name="user_id" value="<%= u.id %>">
                                <button type="submit" class="action-button delete-button" onclick="return confirm('Delete user <%= u.username %>?')">🗑️ Delete</button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>