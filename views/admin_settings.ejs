<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Settings - Investment Crawler</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .settings-section {
            background: #0f1630;
            border: 1px solid #203065;
            border-radius: 8px;
            padding: 25px;
        }
        
        .settings-section h2 {
            color: #f5b042;
            margin-top: 0;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            color: #e8eefc;
            margin-bottom: 8px;
            font-weight: 500;
        }
        
        .form-group input {
            width: 100%;
            padding: 12px;
            background: #1a2a5a;
            color: #e8eefc;
            border: 1px solid #203065;
            border-radius: 6px;
            font-size: 14px;
            box-sizing: border-box;
        }
        
        .form-group input:focus {
            outline: none;
            border-color: #f5b042;
            box-shadow: 0 0 0 2px rgba(245, 176, 66, 0.2);
        }
        
        .save-button {
            background: #f5b042;
            color: #050a12;
            padding: 12px 30px;
            border: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.2s ease;
        }
        
        .save-button:hover {
            background: #ffb347;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(245, 176, 66, 0.3);
        }
        
        .help-text {
            font-size: 13px;
            color: #9ad;
            margin-top: 8px;
            line-height: 1.4;
        }
        
        .security-notice {
            background: rgba(255, 152, 0, 0.1);
            border-left: 3px solid #ff9800;
            padding: 12px;
            margin-bottom: 20px;
            border-radius: 0 6px 6px 0;
            color: #e8eefc;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <header>
        <h1>⚙️ Settings</h1>
        <nav>
            <a href="/admin">Dashboard</a>
            <a href="/admin/keywords">Edit Keywords</a>
            <a href="/admin/urls">Edit URLs</a>
            <a href="/admin/users">Users</a>
            <a href="/admin/settings" class="active">Settings</a>
            <a href="/">← Back to Results</a>
            <form method="POST" action="/logout" style="display:inline;">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button>Logout</button>
            </form>
        </nav>
    </header>
    
    <div class="container">
        <div class="settings-section">
            <h2>⚙️ Default Reset Password</h2>
            
            <div class="security-notice">
                ⚠️ <strong>Security Notice:</strong> This password will be used when administrators reset user passwords. Choose a secure default that users should change immediately.
            </div>
            
            <form method="POST" action="/admin/settings">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                
                <div class="form-group">
                    <label>Default password used by "Reset Password"</label>
                    <input name="default_password" value="<%= currentDefault %>" required type="password">
                    <div class="help-text">
                        This password will be assigned to users when their password is reset. Users should be advised to change it immediately after reset.
                    </div>
                </div>
                
                <button type="submit" class="save-button">💾 Save Settings</button>
            </form>
        </div>
    </div>
</body>
</html>